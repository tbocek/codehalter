The MCP configuration file `.codehalter/mcp.toml` failed to parse: %s.

Read the file, fix the syntax error, and confirm it parses cleanly by re-reading it. Do not start any servers — codehalter will pick up the fix on the next prompt.
