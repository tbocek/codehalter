Summarise this exchange into a structured turn note. Each section = one line, terse, no fluff. Skip sections that don't apply. Reply just the note, no preamble.

Goal: what user wants overall (not just this turn)
Constraints: rules/requirements/preferences user stated, AND any change user explicitly asked to persist — record as locked ("Go upgraded to 1.25 per user — do not revert"), a later turn must never undo it
Tasks: active plan's subtasks, numbered, status + each description verbatim ("1. [done] scaffold module, 2. [doing] wire handler, 3. [todo] add tests"), so a turn post-compaction resumes the plan exactly
Progress: concrete progress this turn (files changed, cmds run, info gathered)
Decisions: choices made or directions taken
Next Steps: what's queued or open
Critical Context: paths, identifiers, versions, or state that must not be lost
