The MCP server %q in `.codehalter/mcp.toml` failed to start: %s.

Inspect its `[[server]]` entry; confirm the command is on PATH and args/env are
right. If its binary is missing, install it and persist in
`.devcontainer/Dockerfile` (see SKILL-container.md).
