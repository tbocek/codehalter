MCP server %q in `.codehalter/mcp.toml` failed to start: %s.

Inspect its `[[server]]` entry; confirm command on PATH + args/env right. Binary missing → install + persist in `.devcontainer/Dockerfile` (see SKILL-base.md).
